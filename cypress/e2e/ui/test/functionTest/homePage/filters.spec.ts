describe('Filters', () => {
    beforeEach(() => {
        cy.visit(Cypress.env('url')); 
    });

    it("should be able to filter by category", () => {

    });

    it("should be able to filter by power tools", () => {

    });

    it("should be able to filter by brand", () => {

    });
});